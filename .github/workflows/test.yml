name: Test get modified files

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print changed files
      run: echo "$(git diff-tree --no-commit-id --name-only -r ${{ github.sha }})"
